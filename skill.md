# 执行摘要

本方案基于《AI Persona Engine》项目需求，设计了一组可在Claude Desktop/Claude Code中直接使用的自定义Skill：**architecture（架构）**、**coding（编码）**、**persona（人格）**、**style（风格）**、**testing（测试）**、**deployment（部署）**。每个Skill均包含SKILL.md文件，提供YAML frontmatter和中文指令内容，引导Claude遵循项目的约定和最佳实践（如模块化设计、代码规范、人格设定、文档风格、测试流程、部署步骤等）。以下将逐一介绍每个Skill的描述、内容示例、使用方法、与CLAUDE.md约束的关系，以及版本/权限/依赖说明，并提供3个示例提示语和典型用例。最后附上安装与使用的简明步骤指南。所有指令均用中文编写，并在必要时引用Claude官方文档或相关资料说明。

---

## Architecture Skill（架构 Skill）

**1) Skill描述与目标：** 指导系统层次和模块划分，确保符合《AI Persona Engine》项目的架构约定。例如，本项目架构分为语音识别、Persona、语音合成、数字人等独立模块，必须接口分离、插件化设计、互不依赖实现细节（遵循CLAUDE.md中“接口必须抽象化、模块独立”等规则）。本Skill在讨论整体设计、模块职责或架构决策时触发，帮助提供标准化指导。

**2) Skill文件内容（SKILL.md）：**  
```markdown
---
name: architecture
description: AI 人格引擎系统架构设计指导。适用于讨论系统总体设计或模块划分时。
---
## 架构指导
- 系统分为语音识别（Speech）、Persona处理、语音合成（Voice）和数字人驱动（Avatar）模块，各模块通过接口通信，职责单一、可替换实现。
- 每个模块应提供清晰接口（API或消息协议）并严格解耦，遵守项目CLAUDE.md中“接口化设计、插件化实现”原则。
- 架构设计应支持实时流式交互和可扩展性。前端和后端分离，后端可部署为Docker服务，前端与用户交互通过WebSocket同步音视频。
- 遵循“高内聚、低耦合”原则，各组件可单独测试和替换，不硬编码依赖（避免在模块内部直接导入其他模块实现）。
```
（**示例输入/输出：** 此Skill主要提供指导文字，无特定输入输出示例。）

**3) 使用示例：** 当用户询问“这个项目的系统架构如何设计？”、“应该如何划分模块？”等与架构相关的问题时，Claude会自动加载该Skill；也可手动通过命令`/architecture`调用。例如，在Claude Desktop输入：  
- `项目需要实时语音交互，如何设计整体架构？` —— 将触发**architecture** Skill，引导回答分层模块设计。  
- `/architecture` —— 直接执行架构Skill，列出系统模块和接口要点。

**4) 与CLAUDE.md交互：** 本Skill内容遵循CLAUDE.md中的架构规范（如“模块必须接口化”、“禁止模块间硬编码”），并将其内嵌在提示中。使用时Claude应遵守Skill指令，设计方案不能违背CLAUDE.md规则。例如，若CLAUDE.md要求“使用插件化接口”，则回答中必须强调采用插件模式。

**5) 版本/权限/依赖：** 该Skill无需额外工具，仅供参考指导，一般Claude Code v2.0+即可使用（无特别权限要求）。前端/后端框架或Docker等依赖由项目本身管理，此Skill仅提供文本说明。可以保持`allowed-tools`为空（默认），不设`disable-model-invocation`，由Claude自动按上下文触发。

**示例Prompt：**  
- “请为AI人格引擎设计系统架构。”  
- “描述本项目各模块的职责和接口。”  
- “如何构建具有可扩展性的模块化架构？”  

**典型用例：** 当开发者询问整体架构或模块划分时，Skill提供参考方案。例如：“我们要添加新功能`语音翻译`，请说明应将其放在哪个模块？”。此时应答应体现架构设计原则，并建议在适当模块中新增功能接口。

---

## Coding Skill（编码 Skill）

**1) Skill描述与目标：** 定义本项目的编码规范和代码生成指导。包括遵循Python开发规范（PEP8）、统一日志记录格式、异常处理约定、文档注释等。当生成或审查代码时，Skill提供要点，确保代码质量与项目要求一致。

**2) Skill文件内容（SKILL.md）：**  
```markdown
---
name: coding
description: 编码规范和最佳实践指导。适用于生成或审查项目代码时。
---
## 编码规范
- 使用 Python3 开发，遵循 PEP8 代码风格（4 空格缩进、变量命名使用 snake_case）并添加必要注释和类型提示。
- 函数接口清晰，参数检查要完备，对可能的异常情况要有处理（必要时使用 try/except 并记录错误日志）。
- 日志使用统一格式（如 `logger.info("msg")`），不直接使用 `print` 输出；敏感信息不写入日志。
- 代码中不得硬编码配置值，需通过配置文件或环境变量传参；避免魔法数字/字符串，用常量或参数代替。
- 遵循项目风格：标识符、函数名和文件名保持一致性（参照CLAUDE.md编码条目），公共函数写Docstring说明功能。
```

**3) 使用示例：** 用户请求生成代码或进行代码审查时自动启用；也可用命令`/coding`手动调用。例如：  
- “请写一个将音频文件转换为文本的函数。” —— 倾向启用coding Skill生成符合规范的代码。  
- “根据本项目风格，改写以下代码段。” 或 `/coding` —— Skill会列出应遵循的编码规范并调整代码。

**4) 与CLAUDE.md交互：** 本Skill指令应遵循CLAUDE.md中的编码要求，例如变量命名、注释、日志格式等（如要求使用UTF-8编码输出提示，遵守项目日志标准等）。Claude在生成代码时必须执行Skill中的规范条目，保证结果符合项目约定。例如，CLAUDE.md可能强调“使用类型注释”，则生成的代码必须包含类型提示。

**5) 版本/权限/依赖：** 该Skill纯文本指导，无特殊版本要求。一般Claude Code v2.x即可正常加载。无需运行外部命令，可将`allowed-tools`留空。不设`disable-model-invocation`，按上下文调用。

**示例Prompt：**  
- “请生成符合PEP8风格的Python函数代码。”  
- “修复以下代码，使其符合项目编码规范：<粘贴代码>”  
- “根据编码标准，为函数`process_data()`添加异常处理和注释。”  

**典型用例：** 当需要编写新模块或修改现有代码时，此Skill提示关键点。例如：“编写一个函数将JSON解析为Python对象。”应答会注明命名、异常、日志等规范；或“该模块需要记录运行状态日志”，应答中会包括如何引入日志记录。

---

## Persona Skill（人格 Skill）

**1) Skill描述与目标：** 人格设计与使用指南。本项目中的Persona指AI顾问角色，Skill指导如何根据persona配置和风格输出内容。要求输出时必须符合目标角色设定，同时附加提醒标注AI身份，不冒充真人。该Skill在涉及创建或使用角色设定（如“扮演某专家”）时触发。

**2) Skill文件内容（SKILL.md）：**  
```markdown
---
name: persona
description: 人格设计与约束指南。适用于设定或使用AI顾问角色时。
---
## 人格与风格指导
- 使用 `personas/` 目录下预定义的角色模板与Prompt。Persona的信息（背景、语气、专业领域）应匹配模板描述。
- 始终包含免责声明：明确“我是一个AI顾问，以下回答为模拟”，避免冒充真实人物。确保不会误导用户（参见伦理约束）。
- 输出语言风格应符合角色设定：如张雪峰风格则要直接、富于比喻；教授风格则正式且详尽等（参考CLAUDE.md中 Persona 部分）。
- 不引入与角色冲突的内容，不透露系统信息，保护用户隐私和敏感数据。
- 如果角色知识涉及高风险领域（医疗、法律等），请追加免责声明并建议咨询专业人士。
```

**3) 使用示例：** 当用户要求“以某某身份回答”或“创建新角色配置”时触发；也可输入`/persona`来查看指南。示例：  
- “请以张雪峰老师的风格回答以下问题。” —— 使用persona Skill确保输出符合该风格并包含AI身份提示。  
- “为即将上线的客服机器人设计一个Persona。” —— Skill指导如何构建角色属性和限制。  
- `/persona` —— 显示人格指南，帮助制定角色方案。

**4) 与CLAUDE.md交互：** 本Skill严格遵循项目的伦理和角色约束条款（如“禁止冒充真实人物”、隐私政策等）。输出中应包含要求的免责声明（例如“我是AI，我的回答仅供参考”），满足项目中关于AI身份标注的规范。任何角色对话都不能违反CLAUDE.md中的守则，如“不得泄露个人敏感信息”。

**5) 版本/权限/依赖：** 该Skill仅文本指导，无需外部工具。Claude Code v2.x即可使用。`allowed-tools`设为空，内容始终提供给Claude参考。可设置`user-invocable: false`（可选）隐藏在命令菜单，仅由Claude根据上下文调用（避免用户误用）。

**示例Prompt：**  
- “为客户服务机器人撰写Persona描述。”  
- “请以专业医生角色回应这条用户提问。”  
- “角色扮演：假设你是张雪峰，请给我选专业建议。”  

**典型用例：** 在需要AI以特定身份回答时此Skill生效。例如：“以金融顾问身份分析股票走势”时，回答应体现此角色背景并遵循上述指导（包括身份声明）。或创建新角色时，提供设定建议和注意事项。

---

## Style Skill（风格 Skill）

**1) Skill描述与目标：** 书面与文档风格指南。本项目输出主要为中文技术文档和代码注释，该Skill规范语言风格和文档格式。包括术语使用、语气要求、格式细节等，保证整个项目文档与交流一致。执行任何编写、润色、翻译任务时应启用。

**2) Skill文件内容（SKILL.md）：**  
```markdown
---
name: style
description: 文档与文字风格指南。适用于格式化文本和文档润色时使用。
---
## 文档和语言风格
- 输出**中文简体**，表达专业、简洁、逻辑清晰，避免口语化和冗余词汇（如“可以说”、“其实”等）。
- 技术文档标题使用层级格式（如 `#` `##`），代码块、示例输入输出使用markdown语法高亮。
- 遵守一致的标点和格式规范：代码中用全角标点，命令或路径用反引号包裹。
- 文本缩进统一为4个空格；中文与数字、英文间加空格分隔；引用符号、列表标号等与项目约定保持一致。
- 回答中提供示例时，请使用项目约定的输出模板（如“步骤列表”采用有序列表）。检查CLAUDE.md中的风格条目要求并严格遵守。
```

**3) 使用示例：** 在需要整理或润色文本时触发。例如：  
- “请检查下列段落的语言风格。”  
- “将这段文档翻译为正式的中文报告风格。”  
- “格式化以下文本，使其符合项目文档规范。”  

也可手动调用`/style`查看风格要求。

**4) 与CLAUDE.md交互：** Skill内容来自CLAUDE.md中的风格/格式规则，并将其应用于输出。例如，若CLAUDE.md要求“使用统一缩进和标点”，Skill会涵盖该要求。Claude在生成输出时必须遵循这些样式规则，包括代码注释和文档格式。任何偏离规范的输出都违反CLAUDE.md。

**5) 版本/权限/依赖：** 该Skill为文本说明，无需运行工具。支持Claude Code v2.x。`allowed-tools`留空。可视为项目共享的语言指南，不设置`disable-model-invocation`，由Claude上下文自动应用。

**示例Prompt：**  
- “请按照规范优化下面的文本：…”  
- “检查并修正文档的格式和用词。”  
- “将以下对话改为正式陈述的文档风格。”  

**典型用例：** 当生成文档、示例说明或帮助文本时。例如输入技术说明或对话，要求更正式或符合规范风格，Skill会确保输出符合上述规范（如自动纠正标点、调整措辞）。

---

## Testing Skill（测试 Skill）

**1) Skill描述与目标：** 单元测试与验证指导。涵盖如何编写并运行测试用例、测试框架和命令等，确保代码质量。主要用于测试相关任务，如生成测试代码、检查测试覆盖率或运行测试时提供规范。

**2) Skill文件内容（SKILL.md）：**  
```markdown
---
name: testing
description: 测试代码和验证指南。适用于需要编写或运行测试用例时使用。
---
## 测试指导
- 使用 **pytest** 框架编写单元测试，命名规则 `test_*.py`，每个函数/类模块对应一个测试函数。
- 编写测试时覆盖正常场景和边界情况。对每个功能点至少写一个测试用例，并使用断言（assert）检查预期输出。
- 在CLI执行测试：示例命令 `pytest --maxfail=1 --disable-warnings -q`。集成在CI中应设置为自动失败。
- 若有数据库或外部服务依赖，使用Mock或临时环境，避免在测试中修改生产配置。
- 测试失败时，Skill指导Claude定位问题并修正代码，同时保持原有功能正确性。
```

**3) 使用示例：** 当用户请求“为某功能编写测试”或“执行测试”时启用。也可通过`/testing`调用。示例：  
- “为语音识别模块写一个单元测试。”  
- “运行所有测试并报告覆盖率。”  
- “修正导致测试失败的代码问题。”  

Skill会提供测试代码生成建议和运行指令。

**4) 与CLAUDE.md交互：** Skill内容应整合CLAUDE.md中的测试策略（如是否在文档中指定特定测试标准）。例如若CLAUDE.md要求使用特定测试数据集或格式，Skill需包含相应说明。任何测试步骤都应符合项目授权范围，如不修改生产数据库。

**5) 版本/权限/依赖：** 可能需要外部工具执行测试，建议在frontmatter中增加`allowed-tools`以自动批准测试运行权限。例如：  
```yaml
allowed-tools: Bash(pytest *)
```
说明Skill可调用pytest执行命令；也可使用`Bash(git *)`等。Claude Code v2.1+支持流式工具执行。设置`disable-model-invocation: true`可防止自动触发，仅手动或上下文显式调用（视项目需求）。  

**示例Prompt：**  
- “为函数`transcribe_audio()`编写pytest单元测试。”  
- “执行测试并输出覆盖率报告。”  
- “测试失败了，帮我定位原因并修正代码。”  

**典型用例：** 在进行代码开发或重构后，需验证功能正确性时使用。例如开发者说“我修改了处理逻辑，请帮我回归测试”，Skill会指导生成或运行相应测试，确保所有用例通过。

---

## Deployment Skill（部署 Skill）

**1) Skill描述与目标：** 构建、打包和部署指南。包括自动化部署脚本、Docker配置、云上部署步骤等。用于部署相关命令，如打包镜像、推送生产环境、运行容器等操作时调用。

**2) Skill文件内容（SKILL.md）：**  
```markdown
---
name: deployment
description: 部署应用程序到生产环境的步骤。只能手动触发。
disable-model-invocation: true
allowed-tools: Bash(docker *) Bash(git *)
---
## 部署步骤
1. **运行测试**：确认所有单元测试和集成测试通过。
2. **构建项目**：根据项目类型运行构建命令（如 `npm run build` 或 `python setup.py install`）。
3. **构建Docker镜像**：示例 `docker build -t persona-engine:latest .`，标签对应版本号或提交号。
4. **推送镜像**：`docker push myrepo/persona-engine:latest`（确保有权限）。或部署到容器编排平台。
5. **更新生产环境**：拉取最新镜像并重启容器，或使用CI/CD工具自动部署。记录部署日志，检查环境变量配置正确。
```

**3) 使用示例：** 本Skill设置为手动命令触发（`disable-model-invocation: true`），用户在Claude中输入`/deployment`即可执行部署指导。示例：  
- `/deployment` —— Skill将输出详细部署步骤清单。  
- “如何用Docker部署项目？” —— Skill会说明构建镜像和运行容器的方法。  
- “生成部署脚本。” —— Skill根据项目情况输出示例脚本。

**4) 与CLAUDE.md交互：** Skill内容必须与项目部署策略一致（如指定的镜像仓库、环境变量规范等）。例如，如果CLAUDE.md中规定了镜像命名规则或特定服务器地址，Skill需要参照执行。禁止在部署脚本中包含未经授权的操作（如私有凭据裸露）。  

**5) 版本/权限/依赖：** 由于涉及实际执行命令，需设置`allowed-tools`允许使用Docker和Git命令（如示例所示）。本Skill标记为手动触发（`disable-model-invocation: true`），避免Claude自动执行；`user-invocable: false`（可选）则不在菜单显示，只能通过命令调用。需要Claude Code v2.1以上支持工具访问权限。部署所需依赖（Docker、git等）需预先安装在环境中。

**示例Prompt：**  
- “帮我生成部署脚本”。  
- “/deployment”  
- “如何在生产服务器上更新应用？”  

**典型用例：** 在准备上线时使用。例如项目经理输入“发布新版本到生产环境”，Skill按照上述步骤检查并执行部署流程。

---

## 安全/伦理约束提示

在上述所有Skill中，必须遵守Claude的安全和伦理要求：尤其在**Persona Skill**和**Style Skill**中提醒，AI生成内容不得冒充真实人物或产生歧视、误导性信息。输出时应显式标注“AI模拟”或类似提示，避免用户误解。同时，遵循项目CLAUDE.md中关于隐私、敏感信息和版权的规范。

---

## 安装与使用指南

以下表格概述了在Claude Desktop/Claude Code环境中安装和使用上述Skill的步骤：

| 步骤 | 操作                          | 命令/示例                             | 备注                                      |
| ---- | ----------------------------- | ------------------------------------ | ----------------------------------------- |
| 1    | 创建Skill目录                  | `mkdir -p .claude/skills/<skill>`    | 在项目根目录创建 `.claude/skills` 子目录；或使用全局目录 `~/.claude/skills/`。 |
| 2    | 添加SKILL.md文件               | 将上述每个Skill的内容复制到对应目录下的 `SKILL.md` 文件中 | 文件名必须严格为 `SKILL.md`；确保使用UTF-8编码保存。 |
| 3    | 启动/刷新Claude                | 重启Claude或运行命令 `/reload-plugins` | Claude会自动加载`.claude/skills/`中的Skill目录。新建目录需重启Claude。 |
| 4    | 验证Skill生效                  | 在Claude对话中输入相关触发语句或命令 | 例如输入“架构设计”，Claude应触发**architecture** Skill。也可直接输入 `/architecture` 等命令。 |
| 5    | 设置DeepSeek替代（如适用）     | 参考示例：                           | 当无法使用Anthropic API时，使用DeepSeek代替：首先安装Claude Code；然后配置：<br>`export ANTHROPIC_BASE_URL=https://api.deepseek.com/anthropic`<br>`export ANTHROPIC_AUTH_TOKEN=<你的DeepSeek密钥>`。<br>之后像使用Claude一样执行 `claude` 命令。 |
| 6    | （可选）其他模型配置          | 如使用Mimo，请参考其文档进行配置        | 例如通过设置 `--model mimo` 或者相关环境变量，让Claude Code使用Mimo模型。具体命令请参阅Mimo官方文档。 |
| 7    | 使用Skill提示和命令            | 输入相应提问或Slash命令              | 使用提示语例：`/coding`、`/testing`等，或直接提问工程相关问题，让Claude自动应用对应Skill。 |

以上步骤完成后，Claude Desktop/Claude Code就能在对应项目中自动使用这些Skill，引导对话遵循项目规范。如有更多Skill或依赖文件，可采用类似方式继续扩展。

**参考资料：** CLAUDE文档及Agent Skills规范。